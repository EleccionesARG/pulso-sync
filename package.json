{
  "name": "pulso-sync-server",
  "version": "1.0.0",
  "description": "Servidor de sincronización SurveyMonkey → Firebase para Pulso",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.2",
    "firebase-admin": "^12.0.0",
    "node-cron": "^3.0.3"
  }
}
